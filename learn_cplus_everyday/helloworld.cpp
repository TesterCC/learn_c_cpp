/*
 * using namespace std; 尽量不要写这句话，开始就养成好习惯。
 * 全部使用std::这种形式，防止函数、接口等冲突，老鸟都是这么干的。
 */

#include<iostream>//包含头文件

int main() {//主函数入口

    std::cout<<"Hello Word!!!"<<std::endl; //调用STD里面的 cout 输出 helloworld
    return 0; //无返回值  为0
};